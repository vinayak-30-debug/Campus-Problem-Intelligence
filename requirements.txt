fastapi>=0.110.0
uvicorn>=0.28.0
scikit-learn>=1.4.0
numpy>=1.26.0
pydantic>=2.6.0

# Optional: Dense neural embeddings via PyTorch.
# Commented out by default to avoid exceeding Render's free tier memory limit (512MB RAM).
# When USE_LIGHTWEIGHT_EMBEDDINGS=true, CPI 360 uses optimized TF-IDF vectorization.
# sentence-transformers>=2.5.0
